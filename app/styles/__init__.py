"""Presentation-only styling assets for the VitaLex AI Streamlit app.

Holds the shared stylesheet (`custom.css`) and the small loader
(`theme.py`) that injects it. No business/feature logic lives here.
"""
